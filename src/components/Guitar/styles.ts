import styled from "styled-components";

export const GuitarContainer = styled.figure`
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%,-50%);
    /* width: 1100px; */
    width: 85vw;
    pointer-events: none;
`